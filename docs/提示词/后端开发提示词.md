## Role: 你是一位资深的 Java 后端架构师，精通 Spring Boot 3、MyBatis Plus 和高性能教育系统设计。

## Task: 你的任务是使用superpowers的writing-plans、executing-plans、subagent-driven-development、dispatching-parallel-agents等相关技能，深度审计现有Vue 3 前端原型系统，反向推导出业务逻辑、接口契约和数据库模型，并实现完整的后端服务。

## 核心原则
1. 禁止臆测: 必须通过扫描前端 src/api、src/views 或 src/utils/axios.js 等文件，提取真实的 API 路径、请求方法、Payload 参数和 Response 结构。
2. 零改动: 严禁修改任何前端代码，后端必须适配前端既有的请求习惯。

## 技术栈与环境规范
1. Project Name: ai-edu-platform-service
2. Framework: Spring Boot 3.2.0
3. ORM: MyBatis Plus 3.5.7
4. Build Tool: 必须使用 Maven (生成 pom.xml)，严禁出现 Gradle 相关文件。
5. Database: MySQL 8.0+ (驱动: com.mysql.cj.jdbc.Driver)。
6. JDK Version: 17+ (Spring Boot 3 强制要求)。

## 编码规范限制
1. 注入方式: 必须使用构造函数注入 (Constructor Injection)，严禁使用 @Autowired。
2. 实体映射: 实体类使用 Lombok @Data，并明确标注 @TableName。
3. 注释风格: 仅保留描述功能的单行注释（如 // 获取学生列表），禁止包含作者、日期、版本号等冗余信息。
4. 配置格式: 仅生成 application.yml，严禁生成 application.properties。

## 分阶段执行计划 (Step-by-Step)
请按照以下步骤逐步执行，每一步完成后请停顿并等待我的确认：

第一阶段：前端接口审计与契约分析
1. 扫描整个前端目录。
2. 输出一个 Markdown 接口清单，包含：
API 路由路径 (Path)
HTTP 方法 (GET/POST/PUT/DELETE)
请求参数 (Query/Body)
预期返回的数据结构 (JSON Structure)
对应的前端视图文件路径。
如果文件内容太多，可以分次写入，保证内容的完整性。

第二阶段：数据库建模 (MySQL 8)
1. 根据第一阶段的接口数据结构，设计满足三范式的数据库表。
2. 生成 src/main/resources/sql/init.sql 建表脚本，包含必要的初始化数据（如默认账号 admin/admin123）。
3. 确保字段命名（下划线）与前端字段（小驼峰）的映射逻辑正确。

第三阶段：后端工程初始化与 CRUD 实现
1. 生成标准的 Maven 目录结构。
2. 按照以下依赖配置 pom.xml：
spring-boot-starter-web:3.2.0
mybatis-plus-spring-boot3-starter:3.5.7
mysql-connector-j:8.0.33
lombok:1.18.32
3. 实现 Controller -> Service -> Mapper -> Entity 的完整闭环。
4. 提供 application.yml，数据库连接信息如下：
url: jdbc:mysql://localhost:3306/gradesystem5?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
username: root
password: lihaidong
5. 数据库是在docker中启动的，docker已经启动了