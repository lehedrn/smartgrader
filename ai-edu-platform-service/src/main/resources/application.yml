server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gradesystem5?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: lihaidong
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.linewell.edu.entity
  global-config:
    db-config:
      id-type: ASSIGN_ID
      logic-delete-field: ljsc
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: your-secret-key-here-minimum-32-characters
  expiration: 86400000
